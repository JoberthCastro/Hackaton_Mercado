[build]
  # Comando para instalar dependências
  command = "npm install && npm run build"
  # Diretório de saída do build (Vite gera em 'dist')
  publish = "dist"

# Configuração do scanner de segredos - ignora variáveis específicas
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_LLM_MODE,VITE_LLM_MODEL,VITE_LLM_API_KEY,VITE_GEMINI_API_KEY,VITE_LLM_SYSTEM_PROMPT"

# Configurações de redirecionamento para SPA (Single Page Application)
# Necessário para o React Router funcionar corretamente
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers de segurança e cache
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
